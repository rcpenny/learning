# add files
git add <filename1> <filename2> ...
git add .

# view changes
git diff (compare working tree with stage)

# view only staged changes
git diff --cached

# delete files
git rm <filename>

# move rename files
git mv <prev_filename> <new_filename>